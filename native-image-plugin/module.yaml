product: jvm/amper-plugin

repositories:
  - https://packages.jetbrains.team/maven/p/amper/amper

dependencies:
  - $libs.amper.jdk.provisioning
  - $ktor.client.cio: runtime-only

pluginInfo:
  settingsClass: Schema

settings:
  ktor: enabled